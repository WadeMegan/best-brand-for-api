CREATE TABLE brand_comments(
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    request_id INTEGER REFERENCES brand_requests(id) ON DELETE CASCADE,
    user_id INTEGER REFERENCES brand_users(id) ON DELETE CASCADE,
    brand TEXT NOT NULL,
    why TEXT NOT NULL
);